using UnityEditor;

// Superseded by ADL > Build Game Scene (Assets/Scripts/Editor/GameSceneBuilder.cs).
// Kept as an empty stub so the file doesn't need to be re-added if Unity reimports it.
public static class SceneSetup { }
